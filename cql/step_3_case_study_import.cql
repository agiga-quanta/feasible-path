////////////////////////////////////////////////////////////////////////////////
//
UNWIND SPLIT($sheets,'#') AS sheet
WITH sheet
  CALL apoc.load.xls($xls_file, sheet)
    YIELD list
WITH COLLECT(list) AS pairs

//
////////////////////////////////////////////////////////////////////////////////

